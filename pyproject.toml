[project]
name = "icon-sheet-studio"
version = "0.1.0"
description = "Convert blurry raster icon sheets into clean, editable SVG icons."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
